# 每日 AI 新聞網頁

這是一個簡單的 Python 網頁程式，會自動抓取並顯示最新的中文 AI 新聞。

## 功能
- 以 Google News RSS 搜尋「人工智慧 / 生成式 AI / OpenAI」關鍵字。
- 顯示新聞標題、來源、發佈時間。
- 啟動後會先抓一次，之後每 24 小時自動更新一次。
- 會將結果快取在 `data/news_cache.json`。
- 不需額外安裝第三方套件（使用 Python 標準函式庫）。

## 啟動方式
```bash
python app.py
```

啟動後開啟：`http://127.0.0.1:5000`
